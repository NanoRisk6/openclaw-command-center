name: Daily Tweet Pipeline\n\non:\n  schedule:\n    - cron: '0 18 * * *'\n  workflow_dispatch:\n\njobs:\n  tweet:\n    runs-on: ubuntu-latest\n    steps:\n      - name: Checkout\n        uses: actions/checkout@v4\n      - name: Tweet Dashboard Status\n        env:\n          OAUTH_TOKEN: ${{ secrets.OAUTH_TOKEN }}\n        run: |\n          curl --fail -X POST https://api.twitter.com/2/tweets \\\n            -H "Authorization: Bearer $OAUTH_TOKEN" \\\n            -H "Content-Type: application/json" \\\n            -d '{\"text\": \"Daily #OpenClaw Treasury Dashboard: https://nanorisk6.github.io/openclaw-dashboard | Stars live! #SOL #NanoRisk\"}'\n